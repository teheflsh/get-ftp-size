# get-ftp-size
Script to get the number of folders and their size on ftp server.

For the script, set these options:

  $ftp_server = "server";
  $ftp_user = "user";
  $ftp_pass = "pass";
  
  $dir = "";
